struct Diagnostic {
  msg: own String,
  level: String
}

def new(msg: own String, level: String) -> Diagnostic {
  Diagnostic { msg, level }
}

def main() {
  let msg = "variable is unused"
  new(msg, "warning")
}
